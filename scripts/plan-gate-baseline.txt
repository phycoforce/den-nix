# Measured expected-local set: pnames that no substituter serves at a
# healthy lock (unfree wrappers, FHS envs, config artifacts). Regenerate
# with: just gate-baseline   Entries matching a BLOCK pattern are never
# written here. Generated 2026-08-01 against e2587caef70c;
# config.toml (noctalia declarative settings render) merged in 2026-08-02.
config.toml
gamescope
mise-bash-completion.bash
noctalia-config
plexamp
spotify
steam
steam-run
thunderbird
